# ملاحظة الدمج

هذا الأرشيف = مشروعك المرفوع (`planforge.zip`) + الدفعة 8أ/8ب.

## ما حدث في `golden.py`

استُبدل المقطع من `# ═══════════════ قياس أثر تغيير رقم ═══════════════`
حتى نهاية الملف بالنسخة الجديدة التي أرسلتِها. بداية الملف (حتى
`run_all`) بقيت كما في رفعتك بلا تغيير.

**تنبيه فرق سلوكي** بين النسخة القديمة والجديدة، لتكوني على علم:

| | القديمة (`impact`) | الجديدة (`compare_with_override`) |
|---|---|---|
| اسم الدالة | `impact()` | `compare_with_override()` |
| العمق المدعوم | أي عدد من مستويات `[key]` متداخلة، عبر `_split_path` | مستوى واحد فقط من `[key]` — `_resolve` تبحث عن أول `[` وتقف عندها بـ`.split("].")[0]` |
| القفل | عالمي: أي `override` نشِط يمنع أي آخر | بمسار واحد: يمنع فقط تكرار *نفس* المسار |
| مصدر القيم | `default_profiles()` (قاموس بجذور متعددة) | يفترض حصرًا جذرين: `uk` و`fix` |

`cli.py` (من الدفعة 8ب) يستدعي `compare_with_override` تحديدًا في
`cmd_codes_impact`، فهو متوافق مع النسخة الجديدة. لكن لو كان أي كود
آخر في مشروعك (خارج ما أُرسل لي) يستدعي `golden.impact(...)` بالاسم
القديم، فسينكسر — يحتاج تحديثًا يدويًا لاستدعاء `compare_with_override`
بدلًا منه، أو تحويل مسارات من نمط `fix.catalogue[WC].w` (عمق أكبر) إلى
ما يدعمه المُحلِّل الجديد.

## ما أُضيف من الدفعة 8أ/8ب (لم يكن في الرفعة الأصلية)

- `edit/ops.py`
- `edit/solve.py`
- `edit/session.py`
- `ranking.py`
- `pipeline.py`
- `project.py`
- `export/svg.py`
- `server/app.py`, `server/static/index.html`, `server/static/app.js`
- `cli.py`

كل ملفات بايثون في الأرشيف فُحصت بـ`ast.parse` وهي سليمة الصياغة، وكل
الاستيرادات التي يستخدمها `cli.py` من `assurance.py` و`codes/*` تحقّقتُ
من وجودها فعليًا في مشروعك المرفوع.

## الدفعة 9 (هذا الأرشيف)

أُضيف:
- `export/dxf.py` — التصدير القابل للتسليم (DXF R2013، طبقات، أبعاد
  DIMENSION حقيقية، فتحات مقطوعة هندسيًا من الجدران).
- `tests/` كاملة: `conftest.py`, `test_provenance.py`,
  `test_usage_map.py`, `test_fixtures_pack.py`,
  `test_reproducibility.py`, `test_geometry.py`, `test_pipeline.py`,
  `test_edit.py`, `test_export.py`, `test_cli.py`, `test_golden.py`,
  و`tests/golden/gc01_bedroom_below_ndss.json`.
- `pyproject.toml`.
- `README.md` مُحدَّث بقسم التصدير ورموز الخروج والحدود.

**تنبيهات معروفة من المرسِل** (راجع رسالة الدفعة 9 الأصلية للتفصيل):

1. `tests/test_usage_map.py::_emitted` يفترض أن
   `planforge.rules.registry.default_registry()` تعرض خاصية `.rules`
   بعناصر لها خاصية `.emits`. إن اختلفت الواجهة الفعلية (من الدفعة 3)
   فالاختبار يتخطّى نفسه (`pytest.skip`) بدل أن يفشل كذبًا — لكنه لن
   يفحص شيئًا حتى يُعدَّل الجامع ليطابق الواجهة الحقيقية.
2. `tests/golden/gc01_bedroom_below_ndss.json` **غير مضبوطة الحقل
   `expect_errors`**: كُتب من قراءة القواعد لا من تشغيلها. أول
   `pytest tests/test_golden.py` سيُظهر الفرق الفعلي؛ صحّح القائمة بعد
   قراءة كل معرّف يظهر، لا بنسخ المخرج مباشرة.
3. الفتحات في DXF تُقطع من الجدران بتقسيم النطاق إلى مستطيلات (بلا
   عمليات بوليانية) — دقيق لأن الفتحات محاذية لمحور جدارها بحكم
   البناء. `PlacedOpening.cut()` غير مستخدم في مسار DXF (يبقى لـSVG
   إن كان مستخدَمًا هناك).
4. مسميات DXF العربية غير مضمونة عبر كل العارضات (تشكيل الحروف
   وربطها يجريان في برنامج العرض لا في الملف). استخدم
   `arabic_labels=False` لمخرج مضمون؛ الحقل `note` في مخرج
   `export_dxf` وفي الملف نفسه يُعلن متى عُطِّلت.
